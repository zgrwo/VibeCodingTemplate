# AGENTS.md — {子项目名} 子目录宪法

> 本文件是 `{{PROJECT_NAME}}` Monorepo 中 `{子项目名}` 子项目的宪法文件。
> 全局架构红线见根目录 [AGENTS.md](../../AGENTS.md)；本文件仅定义子项目特有的约束。
> 术语见根目录 [context.md](../../rules/context.md)。

## 子项目元数据

- **子项目名**：`{子项目名}`
- **语言**：{子项目语言}
- **入口**：`{子项目入口文件}`
- **依赖方向**：→ 可依赖 `{{FOUNDATION_PACKAGE}}`（共享层）；❌ 不可依赖同级其他子项目

## 子项目边界

```
{{PROJECT_NAME}}/
├── src/
│   ├── {子项目A}/        ← 本子项目
│   │   ├── Core/         # 纯逻辑（零外部依赖）
│   │   ├── Service/      # 业务编排（有状态/有依赖）
│   │   └── Udf/          # 外部入口（仅分发适配）
│   ├── {子项目B}/        ← 同级子项目（❌ 不可直接 import）
│   └── foundation/       ← 共享层（✅ 可被任何子项目引用）
└── tests/
    └── {子项目A}/
```

## 红线（在根 AGENTS.md 基础上追加）

| 编号 | 规则 |
|------|------|
| M1 | 子项目不得直接 import 同级其他子项目的内部包 |
| M2 | 子项目间通信必须通过 `foundation/` 共享层或显式接口 |
| M3 | 子项目的 AGENTS.md 变更需通知根项目维护者审查 |
| M4 | 子项目可以有自己的 skills/ 和 rules/ 子目录，但不得与根项目冲突 |

## 与根项目的关系

| 维度 | 根项目 | 子项目 |
|------|--------|--------|
| AGENTS.md | 全局红线 + 架构分层 | 子项目特有约束（追加，不覆盖） |
| skills/ | 通用语言技能 | 可有子项目专属技能（如 `{子项目}-SKILL.md`） |
| rules/ | 全局规范 | 子项目规范在子目录内维护 |
| CI | 统一 ci.yml | 子项目可追加专属 workflow |
| 发版 | release-please 统一管理 | 子项目版本在根 CHANGELOG 中标注 |

## 使用方式

1. 复制本模板到 `src/{子项目名}/AGENTS.md`
2. 替换 `{子项目名}` / `{子项目语言}` / `{子项目入口文件}`
3. 填写子项目特有红线（在根红线基础上追加，不可覆盖根红线）
4. 同步根目录 `rules/project-structure.md` 目录树
5. 同步根目录 `AGENTS.md` 目录树（新增子项目条目）

## 检查清单

- [ ] 子项目边界已在 project-structure.md 中定义
- [ ] 子项目 AGENTS.md 不与根 AGENTS.md 冲突
- [ ] 子项目间无直接 import（通过 foundation/ 或接口通信）
- [ ] 子项目的 skills/rules 不与根项目重复定义
