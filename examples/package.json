{
  "name": "vibe-coding-template-examples",
  "version": "0.1.0",
  "private": true,
  "description": "VibeCodingTemplate examples — TypeScript StatsCore unit tests (vitest)",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
